<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BLOG</title>
  <link rel="stylesheet" type="style" href="/css/style.css">
</head>

<body>
  <div>
  <header>
    <h1>
      Super Rad Tech Blog
    </h1>
   <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="no-button" id="homepage"><a href="/">Home</a></button>
        <button class="no-button" id="dashboard"><a href="/api/user/dashboard">Dashboard</a></button> 
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <button class="no-button" id="homepage"><a href="/">Home</a></button>
        <button class="no-button" id="dashboard"><a href="/login">Dashboard</a></button> 
        <button class="no-button" id="login"><a href="/login">Login</a></button>
        {{/if}}
      </nav>
  </header>
  
 

    <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  </div>
 {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>