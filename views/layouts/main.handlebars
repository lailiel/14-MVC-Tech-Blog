<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BLOG</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>
      Super Rad Tech Blog
    </h1>
    <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="nav-button" id="homepage"><a href="/">Home</a></button>
        <button class="nav-button" id="dashboard"><a href="/api/user/dashboard">Dashboard</a></button> 
        <button class="nav-button" id="logout"><a href="/">Logout</a></button>
        {{else}}
        <button class="nav-button" id="homepage"><a href="/">Home</a></button>
        <button class="nav-button" id="dashboard"><a href="/login">Dashboard</a></button> 
        <button class="nav-button" id="login"><a href="/login">Login</a></button>
        {{/if}}
    </nav>
  </header>
  
  <main class="container">
    {{{ body }}}
  </main>
 
 {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>