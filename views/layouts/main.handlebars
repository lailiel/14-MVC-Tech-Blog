<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BLOG</title>
  <link rel="stylesheet" type="style" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Tech Blog</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <a href="/dashboard/:id" class="btn-no-style">Dashboard</a>
    {{else}}
    <a href="/login" class="btn-no-style">Dashboard</a>
    {{/if}}
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>

    <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>